/**
 * SKIPPED — NichePipeline migrated to MPDrawer panel (PROJ-17 AC-35).
 * Same reason as NichePipeline.test.tsx — original mount-with-props contract gone.
 */
import { describe } from 'vitest';

describe.skip('NichePipelineCards (legacy contract) — migrated to MPDrawer panel', () => {});
