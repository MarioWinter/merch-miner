services:
  web:
    image: ${BACKEND_IMAGE:?BACKEND_IMAGE is required}
    env_file: ./.env
    container_name: app_backend
    command: gunicorn core.wsgi:application --bind 0.0.0.0:${WEB_INTERNAL_PORT:?WEB_INTERNAL_PORT is required} --workers ${GUNICORN_WORKERS:?GUNICORN_WORKERS is required}
    volumes:
      - static_volume:/app/static
      - media_volume:/app/media
    restart: unless-stopped
    networks:
      - default
      - merch_net
      - supabase-net

  worker:
    image: ${BACKEND_IMAGE:?BACKEND_IMAGE is required}
    env_file: ./.env
    restart: unless-stopped
    networks:
      - default
      - supabase-net

  frontend:
    image: ${FRONTEND_IMAGE:?FRONTEND_IMAGE is required}
    container_name: app_frontend
    restart: unless-stopped
    networks:
      - default
      - merch_net

  caddy:
    image: ${CADDY_IMAGE:?CADDY_IMAGE is required}
    container_name: app_caddy
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - static_volume:/srv/static:ro
      - media_volume:/srv/media:ro
      - caddy_data:/data
      - caddy_config:/config
    restart: unless-stopped
    depends_on:
      - web
      - frontend
    networks:
      - default
      - merch_net

volumes:
  static_volume:
  media_volume:
  caddy_data:
  caddy_config:

networks:
  default: {}
  merch_net:
    external: true
    name: merch_net
  supabase-net:
    external: true
