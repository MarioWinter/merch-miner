"""Shared observability helpers (Langfuse handler, Sentry capture).

Moved from `niche_research_app/tasks.py` in PROJ-29 (AC-25). Backwards-compatible
shim re-exports `_get_langfuse_handler` in the legacy location.
"""
