"""Sub-agent tool modules (Phase 2 of PROJ-18).

Each module exposes:
- Individual `@tool`-decorated functions
- A module-level `TOOLS` list for the Phase 3 registry / isolation enforcement

Tool ↔ sub-agent mapping lives in `agent_app.constants.TOOL_AGENT_MAP`.
"""
